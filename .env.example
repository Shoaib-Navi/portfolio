# Canonical site URL for metadata, sitemap.xml and robots.txt.
# On Vercel this is detected automatically — set it only when self-hosting,
# or to pin a custom domain.
NEXT_PUBLIC_SITE_URL=https://your-domain.com
